Seung Lee - A01021720
Partial 2 Exam;

To run the program type
"make"

Then on one terminal type: ./bank_server 8500
On another termianl type: ./bank_client localhost 8500

The options must all appear and work correctly.
At the end, if you do ctrl + shift + c on the server, it'll print the total amount of transactions done in the server while it was opened.